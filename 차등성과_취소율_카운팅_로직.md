# 차등성과(gd) 취소율 카운팅 로직 설명서

> 대상: `wm_dash/index.html`의 차등성과 탭(`gd*` 네임스페이스)
> 최종 갱신: 2026-06-02
> 코드 기준 함수: `gdCountCancels`, `gdCountVanishedCancels`, `gdCountContracts`, `gdGenerate`

---

## 0. 한눈에 보기

차등성과의 **취소수**는 두 갈래의 합산이다.

```
취소수(cancelCount) = gdCountCancels(정상 반품)
                    + gdCountVanishedCancels(사후 배송전 취소)
                    + 회차별 수동 조정(cancelAdj)
```

이 취소수가 다시 등급 배정 기준값인 **취소율반영계약수/계약률** 계산에 들어간다.

```
기준취소율(코디별)  = 취소수 / (실계약수 + 취소수)        ← ※ 아래 5장 주의
취소율반영계약수    = 실계약수 - 취소수                   (refAdjContract, 음수 가능)
취소율반영계약률    = 취소율반영계약수 / DB수             (refAdjRate, 음수 가능 / 등급 기준)
```

> 실제 코드(`gdGenerate`, index.html ~7036행)에서는 `refAdjContract = cont - cx`,
> `refAdjRate = refAdjContract / db`로 직접 계산하며, 별도의 "기준취소율" 변수를
> 코디별로 곱하지 않는다. (취소수를 실계약수에서 그대로 빼는 방식)

---

## 1. 입력 파일과 컬럼 매핑

### 1.1 취소수 산정에 쓰이는 파일

| 타입 | 파일 | 역할 | 보관 |
|---|---|---|---|
| `cancel` | WM_고객관리(취소율) = **신청자관리** | 정상 반품 + 사후 배송전 취소 diff | 누적(직전 회차 비교용) |
| `cont` | WM_고객관리(계약수) | 실계약수(계약+정상취소) 산정 | 누적(최근 ~3개월) |
| `db` | WM_고객관리(DB수) | 회차 분배일 min/max 산출(분모) | 누적 |
| `attend` | 명단(근태/그룹표기) | 멤버 추출 | 최신 1건 |

### 1.2 컬럼 상수

**cancel 파일** — `GD_CANCEL_COL` (0-indexed, index.html:5884)
```
customerKey   : 3   // D열 고객키
manager       : 13  // N열 담당자(코디명, 간혹 "(R_xxx)" 접두 포함)
classification: 16  // Q열 분류(매체코드)
code          : 17  // R열 코드번호 (예: 'IN', 'in_...')
distributeDate: 41  // AP열 분배일
returnDate    : 42  // AQ열 반품일
```

**cont/db 파일** — `COL` (index.html:2273)
```
customerKey   : 6   obName(코디): 7   distributeDate: 8
dbInfo        : 15  mediaCode    : 19  lastCallTime : 21
orderDate     : 23  customerStatus: 29
```

---

## 2. 회차(기간) 정의

- 회차의 시작/끝 = **현재 회차 db 파일의 분배일(COL.distributeDate=8) min ~ max**
  (`gdGenerate`, index.html:6974~6978)
- 시트명(`sheetName`) = 분배일 max를 `YYMMDD`로 포맷 (`fmtDateShort`)
- "현재 회차" cont/cancel 파일 = **db 파일 업로드 시점과 업로드 시각 차이가 가장 작은** cont/cancel 스냅샷 (`gdGenerate`, index.html:6960~6966)
- "직전 회차" 산출
  - **직전 cancel 파일**: 현재 db 파일보다 먼저 업로드된 가장 가까운 db 파일(`_prevDbFile`)을 찾고, 그 업로드 시점과 가장 가까운 cancel 파일 (index.html:6998~7013)
  - **직전 회차 분배 max(`prevMaxDist`)**: IDB results에서 현재 시트명 직전 시트(`YYMMDD` 내림차순)를 파싱 (index.html:6982~6990)

---

## 3. 정상 반품 — `gdCountCancels`

> 위치: index.html:6609
> 시그니처: `gdCountCancels(cancelData, contData, members, periodStart, periodEnd, uploadTime)`
> 호출: `gdCountCancels(cancelFile.data, contFile.data, members, minDistDate, maxDistDate, cancelFile.uploadTime)`

cancel 파일의 각 행을 순회하며 아래 **모든** 조건을 통과하면 해당 코디 취소수 +1.

| 순서 | 조건 | 코드 | 통과 기준 |
|---|---|---|---|
| ① | 행 존재 | `if(!row) continue` | — |
| ② | **반품일 존재** | `returnDt = parseDate(AQ=42)` | 공란이면 제외 |
| ③ | **반품일이 회차 내** | `returnDt < ps \|\| returnDt > pe` | `[분배 min 00:00, 분배 max 23:59:59]` 안 |
| ④ | **분배일 주말·공휴일 제외** | `if(cxDistDate && isHoliday(cxDistDate))` | 분배일(AP=41)이 토/일/공휴일이면 제외 (`holidaySkip`) |
| ⑤ | **코드 'in' 제외(예외 있음)** | 아래 IN 룰 참조 | 'in' 포함 코드 제외, 단 조건부 포함 |
| ⑥ | **담당자 매칭** | N열(13) | 멤버 set 직접 매칭, 실패 시 `(R_xxx)` 접두 제거 후 재매칭 |

- **회차 매칭 기준은 "반품일"**(AQ=42)이다. 분배일이 아니다.
  (휴일 판정만 분배일 기준, 회차 귀속은 반품일 기준 — [project_holiday_rule] 참조)
- cont 파일과의 **고객키 매칭은 하지 않는다.** cancel 파일의 오더일 범위가 cont 스냅샷보다 길어 cont 매칭을 필수로 두면 누락이 큼(과거: 17건 중 15건 매칭 실패).
- 디버그 로그: `[gd] 취소수: 미매칭(이름) N / 분배일 주말·공휴일 N / 코드 in 제외 N`

### 3.1 코드 'in' 제외 + IN 룰 (`gdApplyInRule`)

```
applyIn = (cancel 파일 uploadTime >= 2026-05-18 00:00)   // GD_IN_RULE_CUTOFF
isExactIn = (R열 코드 === 'IN')                            // 대문자 정확히 'IN'
cxClass   = Q열 분류(매체코드) 값

제외 조건:
  cxCode.includes('in')  AND  NOT(applyIn && isExactIn && cxClass)
```

- 즉 코드에 'in'이 들어가면 기본 제외.
- **단 예외**: 2026-05-18 이후 업로드된 파일이고 + 코드가 정확히 `'IN'`이고 + Q열 분류(매체코드)에 값이 있으면 → 제외하지 않고 **취소수에 포함**.
- (2026-05-18 이전 파일에는 이 예외 미적용. `gdApplyInRule` 컷오프)

---

## 4. 사후 배송전 취소 — `gdCountVanishedCancels`

> 위치: index.html:6571
> 시그니처: `gdCountVanishedCancels(prevCancelData, curCancelData, members, cutoffStart)`
> 호출: `gdCountVanishedCancels(_prevCancelFile?.data, cancelFile.data, members, _cancelCutoffStart)`

**원리**: 직전 cancel 파일 D열(고객키)에는 있었는데 이번 cancel 파일 D열에는 **사라진** 고객키 = 시스템에서 행이 삭제됨 = 배송전 취소가 사후에 발생한 것으로 간주.

처리 순서:
1. 이번 cancel 파일의 고객키 전체를 `curKeys` set에 수집.
2. 직전 cancel 파일을 순회:
   - 고객키 공란 → skip
   - **이번 파일에 키가 아직 존재(`curKeys.has(ck)`)** → skip (사라지지 않음)
   - 이미 본 키(`seen`) → skip (중복 방지)
   - **cutoff drop 가드**: 직전 행 분배일(AP=41)이 `cutoffStart`(= cancel 업로드 시점 − 90일) 이전이면 → 자연 만료로 간주, 제외 (`cutoffDrop`)
   - 담당자(N=13) 매칭(직접 → `(R_xxx)` 제거 후 재매칭) 성공 시 +1
3. 디버그 로그: `[gd] 사후 배송전 취소: N건 카운트 / cutoff drop N / 담당자 미매칭 N`

### 4.1 ⚠️ 알려진 불일치 — 분배일 휴일 제외 미적용

> **현재 코드 상태(2026-06-02 기준)**: `gdCountVanishedCancels`에는
> `gdCountCancels`의 ④ "분배일 주말·공휴일 제외" 룰이 **없다.**
>
> 따라서 분배일이 토/일/공휴일인 건이 사후 배송전 취소 경로로 잡히면 취소수에
> 포함된다. (예: 정화정 5/30(토) 분배 건)
>
> 2026-06-02에 이 룰을 추가하는 수정(`if(distDate && isHoliday(distDate)) holidaySkip++`)을
> 적용했다가 **사용자 요청으로 원복**한 상태다. 두 경로의 휴일 처리 정책을 일치시킬지
> 여부는 추후 결정 대상.

### 4.2 한계

- **첫 회차**(직전 cancel 파일 없음) 또는 `prevCancelData`/`curCancelData`가 없으면 빈 결과 반환 → 사후 배송전 취소는 못 잡음(다음 회차부터 작동).
- cutoff drop 가드는 분배일(AP=41)을 오더일 proxy로 사용(cancel 파일에 오더일 컬럼이 정의돼 있지 않음).

---

## 5. 실계약수와의 관계 — `gdCountContracts`

> 위치: index.html:6500
> 시그니처: `gdCountContracts(data, members, uploadTime, cancelData, currentMaxDist, prevMaxDist)`

취소율 계산의 분자/분모를 이해하려면 실계약수 정의가 필요하다.

```
실계약수 = 계약(STATUS_CONTRACT) + 정상취소(STATUS_CANCEL_CHECK AND cancel파일 D열 매칭됨)
```

cont 파일 행별 필터 + 분기:
1. 코디명(obName=7) 멤버 매칭, 오더일(orderDate=23) 존재
2. **분배일(distributeDate=8) 휴일이면 제외** (휴일=분배일 기준)
3. 군자/관리자/삭제요청, 매체 talkis, dbInfo 'in'+무매체 제외
4. 상태 분류(`classifyStatus`) → '계약' 또는 '취소'만 진행
5. **회차 매칭 = 오더일 기준**: `prevMaxDist < 오더일 ≤ currentMaxDist`인 행만 현재 회차
   - 현재 회차 밖(미래/과거)은 제외 (과거 회차 사후 배송전 취소는 `gdCountVanishedCancels`가 별도 처리)
6. 상태가 '계약' → +1
7. 상태가 '취소' →
   - cancel 파일 D열에 고객키 매칭됨(또는 매칭 비활성 모드) → **+1** (정상 반품, 실계약수에 포함)
   - cancel 파일 D열에 없음 → **둘 다 제외** (현재 회차 배송전 취소 신규)

### 5.1 고객상태 분류 (`classifyStatus`, index.html:2306)

```
계약(STATUS_CONTRACT)      : 배송대기, 배송중, 자동구매완료, 구매완료, (+ 'as' 포함)
취소(STATUS_CANCEL_CHECK)  : 반품입고, 승인요청, 입고대기, 환불요청, 환불완료
제외(STATUS_EXCLUDE)       : 결제보류, 상담취소, 상담요청, 상담대기, 결제요청,
                             결제변경후 요청, 신용정보 보류
```

---

## 6. 최종 합산과 등급 기준값 (`gdGenerate`, index.html:7026~7039)

```js
db        = dbCounts[name]                          // 분모(DB수)
rawCont   = contCounts[name]                        // 실계약수(계약+정상취소)
cont      = max(0, rawCont + contractAdj)           // 회차 수동조정 반영
rawCx     = cancelCounts[name]                      // = gdCountCancels + gdCountVanishedCancels
cx        = max(0, rawCx + cancelAdj)               // 회차 수동조정 반영

contractRate   = db>0 ? cont/db : 0                 // 계약률
refAdjContract = cont - cx                          // 취소율반영계약수 (음수 가능)
refAdjRate     = db>0 ? refAdjContract/db : 0       // 취소율반영계약률 (등급 배정 기준)
```

- `refAdjRate`(취소율반영계약률) 내림차순으로 `gdAssignGroups`가 N_A/N_B/N_C/D(H) 분할.
- **회차별 수동 조정**:
  - 실계약수: `localStorage[GD_CONTRACT_ADJ_KEY] = {sheetName:{name:delta}}`
  - 취소수: `localStorage[GD_CANCEL_ADJ_KEY]  = {sheetName:{name:delta}}`
  - 둘 다 해당 회차(sheetName)에만 적용, `max(0, raw+delta)`로 음수 방지.

---

## 7. 핵심 날짜/판정 유틸

| 함수 | 동작 |
|---|---|
| `parseDate(v)` (3394) | Date/문자열/`YYYY[.-/]M[.-/]D` 파싱, 실패 시 null |
| `isHoliday(d)` (2292) | 토(6)·일(0) → true, 아니면 `HOLIDAYS` set 조회 (`YYYY-MM-DD`) |
| `gdApplyInRule(uploadTime)` (5893) | uploadTime ≥ 2026-05-18 → true (IN 포함 룰 적용 여부) |

---

## 8. 디버그 도구

브라우저 콘솔에서:
- `gdDebugCancels(['고객키1','고객키2',...])` — 주어진 고객키가 **정상 반품**(`gdCountCancels`) 경로에서 어떻게 처리/제외되는지 사유(`반품일 공란`, `분배일 휴일`, `코드 in`, `담당자 미매칭` 등)와 함께 추적. (index.html:6653)
  - ※ 이 디버그는 cont/cancel 경로를 재현하며, 사후 배송전 취소(diff)는 별도 안내.
- 일반 콘솔 로그: `[gd] 취소수: ...`, `[gd] 사후 배송전 취소: ...`

---

## 9. 요약 체크리스트

- [ ] 취소수 = 정상 반품 + 사후 배송전 취소 + 회차 수동조정
- [ ] 정상 반품: **반품일** 회차 내 + **분배일** 평일 + 코드 in 제외(IN 예외) + 담당자 매칭
- [ ] 사후 배송전 취소: 직전 cancel 파일에서 사라진 고객키 + cutoff(90일) 가드 + 담당자 매칭
- [ ] ⚠️ 사후 배송전 취소 경로엔 분배일 휴일 제외가 **현재 없음**(원복 상태)
- [ ] 휴일 판정은 항상 **분배일**, 회차 귀속은 종류별(취소=반품일, 계약=오더일, DB=분배일)
- [ ] 등급 기준값 = 취소율반영계약률 = (실계약수 − 취소수) / DB수
